defmodule BotArmyCodingAgentProxy do
  @moduledoc """
  NATS-facing proxy for coding agents.

  This bot accepts request/reply calls from coding-agent clients and forwards
  them to the Bot Army LLM lane subjects over NATS.
  """
end
